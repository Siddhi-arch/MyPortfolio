# Weather Dashboard

A responsive weather dashboard built with HTML, CSS & JavaScript.  
It uses the OpenWeatherMap API (needs a valid API key) to show current weather.

## Usage

1. Enter a city name.
2. Click “Search”.
3. View temperature, humidity, wind & weather icon.

## How to run locally

1. Clone the repo
2. Open `index.html` in browser
